<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TutorManagementiOS.RegisterCoursePage">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Orientation="Vertical" Padding="20" Spacing="20">
                <BoxView HeightRequest="10"/>
                <Frame BackgroundColor="#BF043055" HasShadow="False">
                    <StackLayout Orientation="Vertical" Spacing="10">
                        <Entry x:Name="cName" Text="{Binding courseName}" Placeholder="Enter Course Name"   
                           PlaceholderColor="White" HeightRequest="40"   
                           TextColor="White"/>
                        <Entry x:Name="cNumber" Text="{Binding courseNumber}" Placeholder="Enter Course Number"   
                           PlaceholderColor="White" HeightRequest="40"   
                           TextColor="White"/>
                    </StackLayout>
                </Frame>
                <!--<Button x:Name="btnCourseSubmit" Clicked="btnCourseSubmit_Clicked" Text="Register" TextColor="White"  
                    FontAttributes="Bold" FontSize="Large" HorizontalOptions="FillAndExpand"  
                    BackgroundColor="#BF043055" />-->
                <Label x:Name="test"/>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>